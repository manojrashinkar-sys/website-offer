# Served as /robots.txt on website-offer.manojrashinkar.com.
# There is no public/robots.txt on purpose: Vercel serves static files before
# it applies rewrites, so a file at that path would win and the Web Services
# subdomain could never be given a different one. See vercel.json.

User-agent: *
Allow: /

# The Web Services pages are reachable here so they can be opened on preview
# deployments, which are not the subdomain. The subdomain is their real home
# and the only indexable copy, so this one stays closed.
Disallow: /community

Sitemap: https://website-offer.manojrashinkar.com/sitemap.xml
